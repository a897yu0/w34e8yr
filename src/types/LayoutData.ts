
interface LayoutData {
  adminPage: {
    sidebar: {
      width: number;
    };
    serverManagementPanel: {
      serverTable: {
        itemCountPerPage: number;
        height: number;
      };
    };
  };
}

export type { LayoutData };
